version: "3.1"

services:

  db:
    image: mysql:latest
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    ports:
      - "3306:3306"
    expose:
      - "3306"
    environment:
      MYSQL_ROOT_PASSWORD: rp
      MYSQL_USER: user
      MYSQL_PASSWORD: password
      MYSQL_DATABASE: blueocean2
    volumes:
      - data:/data/db

volumes:
  data:
